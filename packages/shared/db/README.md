# packages/shared/db

Sole owner of the Drizzle schema, migrations and RLS policy files (CONVENTIONS §4).
Story 1.3 creates the class-1 truth and class-2 operational tables here; until then this
directory is intentionally empty so `pnpm rebuild` stays honestly unarmed.
